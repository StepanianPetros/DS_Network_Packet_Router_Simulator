namespace ConsoleApp1
{
    // Entry point: Creates CLI and starts the router simulator
    class Program
    {
        static void Main()
        {
            var cli = new CLI();
            cli.Start();
        }
    }
}